//export class if defining function
//export interface if declaring function without code
export interface confirmDelete {
    objectCode: number;
    objectID: number;

    confDel(objectCode, objectID);
        //console.log("layeradmin component > confirmDelete model > confirmDelete component")
    
}